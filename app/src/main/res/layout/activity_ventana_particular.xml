<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- El marco principal -->
    <RelativeLayout
        android:id="@+id/marco_principal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/fondo_contenido">

        <ListView
            android:id="@+id/lista_items"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@android:color/transparent"
            android:dividerHeight="0dp" >
        </ListView>

    </RelativeLayout>

    <!-- El menu desplegable --><!-- android:background="@color/fondo_menu_desplegable" -->
    <ListView
        android:id="@+id/menu_desplegable"
        android:layout_width="290dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:choiceMode="singleChoice"
        android:dividerHeight="0dp"
        android:divider="@color/negro"
        android:clickable="true"
        android:background="@color/fondo_menu_desplegable" />
</android.support.v4.widget.DrawerLayout>